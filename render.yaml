services:
  - type: web
    name: lineup-lab
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: ADMIN_PASSWORD
        sync: false
