# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
test-results
.claude
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
server/data/

# test scratch output
.tmp-tests

# Local secrets. NEVER commit these: .env holds the Supabase service-role key,
# which bypasses row-level security. .env.example is the safe, value-free
# template and is deliberately allowed through.
.env
.env.*
!.env.example
