/**
 * What the book says while it works.
 *
 * One list, because two screens perform this ritual now: the curtain a
 * connected league raises on the way in, and the landing page, where it is the
 * product's first performance in front of somebody who has never seen it.
 * Two copies of these words would drift, and they are the voice.
 */
export const PRICING_LINES = [
  'Setting the line',
  'Balancing the book',
  'Reading every roster',
  'Finding the edge',
  'Moving the market',
  'Locking the number',
] as const;
